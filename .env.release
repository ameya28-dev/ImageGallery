# Release mode configuration
# Loaded automatically by docker compose when using compose.release.override.yaml
# This file ensures Swagger and debug endpoints are disabled

# For local profile: local,release
# For feature profile: override in the script
SPRING_PROFILES_ACTIVE=local,release
